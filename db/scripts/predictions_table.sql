CREATE TABLE CULTURE_PREDICTION(
    CENTROID VARCHAR(50) NOT NULL,
    LONGITUDE DOUBLE PRECISION NOT NULL,
    LATITUDE DOUBLE PRECISION NOT NULL,
    YEAR INT NOT NULL,
    CULTURE_CODE VARCHAR(10) NOT NULL,

    PRIMARY KEY (CENTROID, YEAR)
);